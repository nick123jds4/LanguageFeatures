@model IEnumerable<string>
<div class="bg-info">
    This is the message from the partial view
    <ul>
        @foreach (string name in Model)
        {
            <li>@name</li>

        }
    </ul>
</div>
